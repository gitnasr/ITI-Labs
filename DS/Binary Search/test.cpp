#include <iostream> 
#include <stack>
using namespace std;

class Solution {
public:
    bool isValid(char* s) {
        stack<char> stack;
        stack.push(34);
        return true;
    }
};

int main() {
    Solution s = Solution();
    s.isValid("[)]");
}